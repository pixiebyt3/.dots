[include]
  path = /home/pixiebyte/.gitconfig.local
[core]
	editor = nvim
[credential]
  helper = cache --timeout=3600
[color]
  ui = true
[init]
	defaultBranch = develop
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
